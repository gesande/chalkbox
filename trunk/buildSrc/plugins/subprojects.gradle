import net.sf.chalkbox.build.DefaultContinousBuildPlugin

apply plugin : DefaultContinousBuildPlugin
forkJdt { jdtInputFile = "${project.properties.buildTemplates}/for/eclipse/org.eclipse.jdt.core.prefs" }

